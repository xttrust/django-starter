# Local Development Environment Variables
# Copy this file to .env.local for local development without Docker

# Django Settings
DEBUG=1
SECRET_KEY=django-insecure-local-dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration
# DATABASE_URL is NOT set - this forces Django to use SQLite locally

# Redis Configuration (optional for local development)
# REDIS_URL=redis://localhost:6379/0

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Local development notes:
# 1. No DATABASE_URL = SQLite will be used automatically
# 2. Redis is optional for local development
# 3. Email will be output to console instead of sending real emails